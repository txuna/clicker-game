apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-redis.fullname" . }}-test-connection"
  labels:
    {{- include "log-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
