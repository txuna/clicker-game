apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-redis.fullname" . }}
  labels:
    {{- include "log-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-redis.selectorLabels" . | nindent 4 }}
